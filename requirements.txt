# AI Short Factory Dependencies

# Web UI
flask>=3.0.0

# HTTP Client for llama-server communication
requests>=2.31.0
urllib3>=2.0.0

# Image processing
Pillow>=10.0.0

# PyTorch (for WAN2.2 - install separately with CUDA support if needed)
# On Windows GPU: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0

# Video processing (ffmpeg must be installed separately on system)
# For Windows: Download from https://www.gyan.dev/ffmpeg/builds/

# Utilities
python-dateutil>=2.8.2
json-repair>=0.25.0  # JSON auto-repair for LLM output stability

# Testing
pytest>=8.0.0
